# TASK: extract `company` section labels

Extract structured company-description fields from the block range below.

## Title (verbatim, source language — provides context)

{{ title_raw }}

## Section text ({{ section_blocks|length }} blocks)

{% for b in section_blocks -%}
[{{ b.id }}] ({{ b.tag }}) {{ b.text }}
{% endfor %}

{% include '_partials/english_normalize.md.j2' %}

## Fields

- `industry_tags` — array of free-text English tags, typically 1–3. Examples: `"fintech"`, `"pharmaceuticals"`, `"industrial manufacturing"`, `"retail banking"`, `"public transit"`, `"hospitality"`, `"defence"`, `"legal services"`, `"agriculture"`.
- `size_band` — one of: `"1-10"`, `"11-50"`, `"51-200"`, `"201-500"`, `"501-1000"`, `"1001-5000"`, `"5001-10000"`, `"10001+"`, or `null` if not stated.
- `funding_stage` — one of: `"bootstrapped"`, `"seed"`, `"series_a"`, `"series_b"`, `"series_c"`, `"series_d_plus"`, `"public"`, `"acquired"`, `"family_owned"`, `"cooperative"`, `"state_owned"`, `"subsidiary"`, `"mutual"`, `"nonprofit"`, `"government"`, `"unknown"`, or `null` if not stated.
- `mission_verbatim` — one-sentence mission statement, **verbatim source language**, or `null` if absent.

## Output

Write JSON to `{{ output_path }}`:

```json
{
  "industry_tags": [...],
  "size_band": null,
  "funding_stage": null,
  "mission_verbatim": null
}
```

All four fields are required in the output (use `null`/`[]` when unknown).

{% include '_partials/json_only.md.j2' %}

{% if previous_error -%}
## Previous attempt failed

```
{{ previous_error }}
```
{% endif %}
