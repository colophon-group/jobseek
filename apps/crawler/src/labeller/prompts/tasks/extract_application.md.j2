# TASK: extract `application` section labels

## Title (context)

{{ title_raw }}

## Section text ({{ section_blocks|length }} blocks)

{% for b in section_blocks -%}
[{{ b.id }}] ({{ b.tag }}) {{ b.text }}
{% endfor %}

## Fields

- `application_deadline` — ISO date (`YYYY-MM-DD`) if the posting states a clear closing date, else `null`.

## Output

Write JSON to `{{ output_path }}`:

```json
{
  "application_deadline": null
}
```

{% include '_partials/json_only.md.j2' %}

{% if previous_error -%}
## Previous attempt failed

```
{{ previous_error }}
```
{% endif %}
