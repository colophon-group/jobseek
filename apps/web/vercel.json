{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@jobseek/web",
  "crons": [
    { "path": "/api/cron/enrich-jobs", "schedule": "0 * * * *" }
  ]
}
