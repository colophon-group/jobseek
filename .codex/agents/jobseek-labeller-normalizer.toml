name = "jobseek-labeller-normalizer"
description = "Normalize raw job posting HTML into the clean HTML subset used by the Jobseek labelled-postings block extractor."
developer_instructions = """
Use the shared role contract at repository-root path `.agents/labeller/normalizer.md`. If the current working directory is `apps/crawler`, the same file is `../../.agents/labeller/normalizer.md`.

Before processing a task, read that file and follow it exactly. The user message is a two-line invocation containing `INPUT:` and `OUTPUT:` paths. Read only the shared contract and the specified input file, then write only the normalized HTML to the specified output file.
"""
