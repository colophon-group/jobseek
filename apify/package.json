{
  "name": "jobseek-apify-actors",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "actors/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.4.5"
  }
}
